{
  "name": "lucas-utils",
  "version": "0.0.1",
  "author": "stardustrain <kthanterran@gmail.com>",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stardustrain/lucas-utils.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "yarn --frozen-lockfile && yarn build:packages",
    "build": "lerna run build",
    "build:packages": "yarn build",
    "changeset": "changeset",
    "release": "changeset publish"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.16.0",
    "tsdx": "^0.14.1",
    "tslib": "^2.3.0",
    "typescript": "^4.3.5"
  }
}
